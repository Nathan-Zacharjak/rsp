#include <array>
#include <bits/unique_ptr.h>
#include "array_list.h"

template <typename T>
void array_list<T>::insert(T value)
{
}

template <typename T>
T &array_list<T>::at(int index)
{
}

template <typename T>
void array_list<T>::remove(int index)
{
}
